/*
=========================================================================================================================
Write the code of the function that will display the bit-pattern of a given intger.

Note: - Use bit manipulation operators if applicable.
      - The code should be platform independent. (when using an 8-bit, 16, 24, 32, 64-bit system, it wouldn't matter,
      it'd still work)
      [Platform independent Hint: Use sizeof operator determine the number of bits representing the integer variable]
      - No array should be used in this function.

Given integer:
      ======= 24 =======
      in an 8-bit system
=========================================================================================================================
*/

#include<stdio.h>
#include "functions.h"

int main(){
      int elem = 129;

      displayBit(elem);

      return 0;
}


















